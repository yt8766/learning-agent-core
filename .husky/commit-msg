#!/usr/bin/env sh

export NODE_OPTIONS="${NODE_OPTIONS:---max-old-space-size=8192}"
pnpm exec commitlint --edit "$1"
VERIFY_INCLUDE_WORKTREE=0 VERIFY_INCLUDE_STAGED=1 VERIFY_INCLUDE_UNTRACKED=0 pnpm verify:affected
pnpm test:workspace:smoke
