# app

Target Nest composition root for the unified backend. During migration, the active root still lives in `src/app.module.ts`; new bootstrap setup, module wiring, global filters, global pipes, and lifecycle configuration should move here as tasks land.
