# 工作流记录目录

状态：current
文档类型：index
适用范围：`docs/workflows/`
最后核对：2026-04-27

本目录用于沉淀当前仍可参考的工作流记录、执行流转说明和计划索引。历史迁移台账与不再代表当前实现的资料放在 [docs/archive](/docs/archive/README.md)。

当前文档：

- 当前目录索引就是工作流记录导航主文档。
- 历史迁移与旧计划归档入口：[docs/archive/README.md](/docs/archive/README.md)

当前约定：

- 当前真实实现说明优先放在对应 `docs/apps/`、`docs/packages/`、`docs/contracts/`、`docs/integration/` 或 `docs/conventions/`。
- 已完成且只保留历史价值的计划、迁移台账和旧设计应进入 `docs/archive/`，并标记 `状态：archive`、`history` 或 `completed`。
