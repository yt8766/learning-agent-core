# platform-runtime demo

本目录承载 `@agent/platform-runtime` 的真实最小闭环验证。

- 入口：`pnpm --filter @agent/platform-runtime demo`
- 目标：验证 default platform runtime 会注入官方 agent registry、workflow presets 与 metadata
