# **TITLE**

由 `@agent/tools` 的 scaffold 能力生成的通用 agent 骨架。

## Validation

- Type: `pnpm typecheck`
- Spec: `pnpm test:spec` + `src/flows/__NAME__/schemas/__NAME__.schema.ts`
- Unit Test: `pnpm test`
- Integration: `pnpm test:integration`
